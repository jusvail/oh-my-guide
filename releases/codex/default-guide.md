Use the installed `$oh-my-guide` Skill to guide and execute project work unless the user explicitly selects another workflow.
Answer one-off questions, reviews, and advice requests directly without task files or implementation. For a clear
implementation request, resolve material design questions and continue through execution and proportional validation
under the user's existing authority. Explicit guide/redesign requests remain design-only until implementation is requested.

Do not ask the user to enter a Skill name, command prefix, or bootstrap prompt, and do not announce activation.
Natural-language implementation requests authorize the same work as task commands. Maintain durable tasks only when
cross-session recovery is useful; existing task IDs and explicit utility modes remain available.
